name: Angular Test Coverage

on: [ push, pull_request ]

jobs:
  front-end-ci:
    name: Angular CI/CD
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./front
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Install Chrome dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libnss3 xvfb

      - name: Run tests with coverage
        run: xvfb-run --auto-servernum --server-args="-screen 0 1024x768x24" npm run test -- --browsers=ChromeHeadless --code-coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/




      # IMAGE ET PUSH DOCKERHUB
#      - name: Build Docker Image
#        working-directory: ./front
#        run: docker build -t ncazo/op-10_front:latest .
#
#      - name: Log in to Docker Hub
#        uses: docker/login-action@v2
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_TOKEN }}
#
#      - name: Push Docker Image
#        run: docker push ncazo/op-10_front:latest